[Default]

work_dir = _workspace

dataset_dir = _dataset

extraction_dir = %(work_dir)s/features/w2048

model_dir = %(work_dir)s/models

log_dir = %(work_dir)s/logs

prediction_dir = %(work_dir)s/predictions


[Extraction.Logmel]

n_fft = 2048

hop_length = 1002
