[Default]

work_dir = _workspace

dataset_dir = _dataset

extraction_dir = %(work_dir)s/features/w1024

model_dir = %(work_dir)s/models

log_dir = %(work_dir)s/logs

prediction_dir = %(work_dir)s/predictions


[Extraction.Logmel]

n_fft = 1024

hop_length = 512


[Training]

pseudolabel_path = metadata/pseudolabels.csv
